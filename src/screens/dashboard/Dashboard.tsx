import { View, Text } from 'react-native';

const Dashboard = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center' }}>
            <Text>Dashboard</Text>
        </View>
    );
}

export default Dashboard;
